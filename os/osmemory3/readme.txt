内存管理

利用连续分配内存管理中的动态分区分配算法，实现数据文件中的内存请求。
要求实现首次适应、循环首次适应、最佳适应、最坏适应，并且评估这四个算法的效率。

数据文件格式：
n L
Q1 D1 L1
Q2 D2 L2
...
Qn Dn Ln

n: 文件共有 n + 1 行
L: 总内存，以字节为单位
Qi: 请求时间，以秒为单位
Di: 占用内存的时间，以秒为单位
Li: 内存需求，以字节为单位
